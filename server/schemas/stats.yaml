# ─── 统计格式模型 ───
# 职责：数据统计/聚合响应格式
# 本文件为 openapi.json schemas 的按职责拆分参考文件
# FastAPI 自动生成 OpenAPI 规范，不直接加载此文件
# 如未来迁移至手动管理的 OpenAPI 规范，可将此 $ref 引入

StatsResponse:
  type: object
  description: "数据统计响应模型——聚合统计格式"
  properties:
    total_trends:
      type: integer
      description: "总数据条数"
    sources:
      type: object
      description: "各来源数据量分布"
    heat_levels:
      type: object
      description: "各热度等级数据量分布"
    avg_heat:
      type: number
      description: "平均热度值"
    max_heat:
      type: integer
      description: "最高热度值"
    categories:
      type: object
      description: "各分类数据量分布"
    last_updated:
      type: string
      description: "最后更新时间"
